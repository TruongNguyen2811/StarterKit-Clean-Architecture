import 'package:flutter/material.dart';

const List<Locale> SUPPORTED_LOCALES = [Locale('vi'), Locale('en')];
const Locale DEFAULT_LOCALE = Locale('vi');

const Map<String, String> LANGUAGE_MAP = {
  'vi': 'Tiếng Việt',
  'en': 'English',
};
